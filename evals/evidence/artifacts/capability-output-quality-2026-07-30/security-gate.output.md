[Trace: Guyue/SecurityGate] 对明确目标执行本地启发式预检；不把未命中等同完整安全

结论：**Red，阻断执行。**

可观察风险：

- 远端响应被直接交给 `bash` 执行，脚本内容、来源完整性与实际权限均未知。
- 未固定版本、commit 或 SHA-256，无法确认每次下载的是同一内容。
- `curl -s` 隐藏错误信息，且未使用失败即退出选项，降低异常可见性。

最小 E4 收据：

- 目标来源/URL：`https://example.invalid/install.sh`
- 目标版本/指纹：缺失
- 扫描命令与退出码：未运行；按要求不联网、不执行
- 命中项：未知远端脚本直通 Shell、无完整性校验、静默下载
- 人工判断：不得放行或执行；不推断作者意图
- 未覆盖边界：脚本实际内容、重定向与响应、签名/哈希、所需权限及副作用

最小下一步：提供脚本的本地静态副本，以及可信来源、固定版本或 commit、SHA-256 和预期权限说明；随后仅对该本地副本做安全预检。
