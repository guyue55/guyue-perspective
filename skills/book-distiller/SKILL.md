---
name: book-distiller
description: |
  仓颉方法论提炼器 (The Cangjie Distillation Process)。将长篇书籍、商业实战或方法论文章提炼成清晰、可执行的 AI Skill 架构。
  当用户要求“总结这套方法论”、“提取这本书的框架”、“把这个文档转化成工作流”时使用。不能用于生成普通的文本摘要。
---

# book-distiller (The Cangjie Distillation Process)

> 一句话钩子：不写空洞的读后感，只榨取长文里能变成代码和机器指令的决策树。

## 你什么时候需要它？
- 读完一本管理/技术书籍，希望把作者的方法论变成自己 Agent 的永久技能。
- 手头有一份冗长的业务 SOP，想把它结构化为机器可执行的工作流步骤。

## 触发方式
- "用 book-distiller 把这篇长文提炼成一个技能"
- "提取这个PDF里的决策框架，按 RIA++ 格式输出"
- "把这本关于增长黑客的书结构化，提炼出检查清单"

## 反例黑名单 (Anti-Slop Guardrails)
- **非方法论拦截**：如果是小说、散文、闲聊文本，直接拒绝执行并提示：“当前文本属于叙事/抒情类，不包含可提炼的结构化方法论”。
- **禁止废话**：严禁输出类似“这本书讲述了...的道理”的读后感。必须输出冷酷的、结构化的组件块。

## RIA-TV++ 核心工作流
1. **骨架抓取 (Adler)**：通读长文，建立知识骨架，提取专属名词，输出 `BOOK_OVERVIEW.md`。
   - 记录书名、作者、版本/出版信息、输入文件或官方来源、章节/页码或位置标识；无法定位的观点不得伪造精确出处。
2. **候选轨迹与早期校准**：先建立 `candidate / verified / rejected` 三栏；每个候选记录来源位置、适用边界、验证样本和拒绝原因。在骨架与首批候选形成后先让用户校准目标粒度、领域边界和预期用途，避免整篇提炼后才发现方向错误。
3. **三重验证 (Triple Verification)**：
   - *交叉验证*：有两处以上独立支持吗？
   - *预测力*：能解决书外的新问题吗？
   - *唯一性*：是不是连大模型都知道的常识废话？（常识一律剔除）
   - 三项都通过才从 `candidate` 进入 `verified`；失败、常识化或与现有 Skill 重复的条目进入 `rejected`，保留理由而不是静默删除。
   - 每条进入交付的方法必须就近保留章节、页码或输入位置锚点；若把原文原则扩展为适用范围、模板或新步骤，明确标为“模型推演/待验证”，不得让执行化表达伪装成作者原意。
4. **组装 RIA++**：
   - **R (原文)**：只保留完成分析所需的最短合规摘录，并附章节/页码或位置；受版权限制或来源不允许长引文时改用转述，不用固定字数上限替代来源规则。
   - **I (转译)**：用行动指令重写框架。
   - **A1/A2 (应用)**：过去案例与未来触发此技能的条件。
   - **E/B (执行与边界)**：123步的执行链路，与该方法论失效的边界条件。
   - 每条可执行方法的最小交付字段为：来源锚点、触发条件、步骤、检查信号、反模式和适用边界；字段缺失时仍是候选笔记，不得标为完成蒸馏。
5. **归档测试**：生成 `test-prompts.json`，覆盖正向、诱饵、负向、书外新样本和相邻 Skill 混淆；比较吸收前后质量、Token 与误路由，未证明净增益的内容不进入核心 Skill。
